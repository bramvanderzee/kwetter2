apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ingress
spec:
  rules:
  - host: api.localhost
    http:
      paths:
      - path: /api/profile
        pathType: Prefix
        backend:
          service:
            name: profile-service
            port:
              number: 3000
